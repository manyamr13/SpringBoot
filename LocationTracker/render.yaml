services:
  - type: web
    name: LocationTracker
    env: java
    buildCommand: ./mvnw clean install
    startCommand: java -jar target/LocationTracker-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 21